apiVersion: v1
kind: Service
metadata:
  name: {{ include "f1-webapp.fullname" . }}
  labels:
    {{- include "f1-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "f1-webapp.selectorLabels" . | nindent 4 }}
